alias start="docker-compose up"
alias install="docker exec -w /var/www php composer install"
alias build="docker-compose build"
alias down="docker-compose down"
alias mysql="docker exec -it mysql /bin/bash"
alias bash="docker-compose exec php /bin/sh"

